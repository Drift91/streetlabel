config = {
	color = {r = 240, g = 200, b = 80},
	position = {
		x = 16.45, -- distance across the x-axis, from left to right (default: 17.55)
		y = 1.25,	   -- distance across the y-axis, from bottom to top (default: 3)
	},
	vehicleCheck = false,
}